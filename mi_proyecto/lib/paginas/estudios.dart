import 'package:flutter/material.dart';

class Estudios extends StatelessWidget {
  const Estudios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Página de Estudios'),
    );
  }
}
